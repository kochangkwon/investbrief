.env
*.db
*.db.bak-*
__pycache__/
*.pyc
.venv/
node_modules/
.next/

# 런타임 산출물 (수급 캐시·서비스 로그)
backend/runtime/
backend/logs/

# 백업 파일 (절대 커밋 금지)
*.bak
*.backup
*.backup_*
*.db.backup*
*.py.backup*
.env.backup
.env.bak

# 로컬 환경변수 시크릿 (절대 커밋 금지)
.env.local
*.env.local

# OS 잡파일
.DS_Store

# DART corpCode 로컬 캐시 (7일 TTL로 자동 재생성)
backend/data/dart_corpcode.json
