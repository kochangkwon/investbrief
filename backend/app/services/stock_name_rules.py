"""종목명 추출 공통 규칙 — discovery/radar 양쪽에서 공유.

뉴스 텍스트에서 종목명 후보를 추출할 때 거르는 불용어와
단독 등장 시 지주사로 오탐되는 그룹명 집합.
"""
from __future__ import annotations

STOPWORDS = {
    "한국", "미국", "중국", "일본", "유럽", "코스피", "코스닥",
    "증시", "시장", "투자", "기업", "정부", "대통령", "장관", "위원회",
    "분석", "전망", "예상", "발표", "공시", "뉴스", "기사", "매출", "실적",
    "영업이익", "순이익", "주가", "주식", "종목", "거래", "상승", "하락",
    "오늘", "내일", "어제", "금주", "이번", "지난", "최근",
    # ── 본문 추출 빈출 노이즈 보강 ──
    "수혜주", "관련주", "대장주", "테마주", "급등", "급락", "강세", "약세",
    "호황", "불황", "규제", "차세대", "역시", "장악", "초집중", "주도",
    "확대", "성장", "둔화", "회복", "부진", "개선", "전반", "당분간",
}

# 한국 주요 그룹명 — 단독 등장 시 지주사로 잘못 매핑되므로 차단
# (그룹명 + 후속 단어 결합한 종목명은 정상 매칭됨, 예: "한화에어로스페이스")
GROUP_PREFIX_NAMES = {
    "삼성", "LG", "현대", "SK", "롯데", "한화", "한국", "GS",
    "CJ", "두산", "포스코", "효성", "한진", "신세계", "농심",
    "오리온", "동원", "코오롱", "대상",
}


# ── 일반명사와 동철(同綴)인 상장사명 (2026-08-26, 오태깅 실사고) ──
# "대상"(對象)은 뉴스 본문에 일상어로 매일 등장한다. 실측(theme_detection):
# 이틀간 4회 오추출 — "상장사 대상"(IPO 기사), "환자를 대상으로"(제약 기사) 등.
# Claude 검증이 3건은 걸렀으나 1건(기술수출 테마)이 YES로 새어 실매수 직전까지
# 갔다(2026-08-26 001680, 개장 전 수동 취소). 검증 LLM에 위임하기엔 노출 빈도가
# 너무 높으므로, 추출 단계에서 "회사 문맥" 증거를 요구한다.
import re as _re

# 2026-08-26 사고 후 실측 확장 (theme_detection verdict 통계 + 오추출 문맥 확인).
# 판정 근거는 "NO 누적 + 여러 테마에 무차별 매칭 + 헤드라인이 아닌 본문에서 추출".
# 신규 후보는 추측하지 말고 scripts/surface_ambiguous_names.py 로 산출할 것.
AMBIGUOUS_COMMON_NOUN_NAMES = {
    "대상",      # 對象 — NO 18건/11개 테마. "환자를 대상으로", "상장사 대상" (실사고)
    "동원",      # 動員 — "자금 동원", "총동원"
    "전방",      # 前方 — NO 6건/5개 테마. "전방 산업", "전방 시장" (산업 기사 상용구)
    "나노",      # nano — NO 6건/5개 테마. "2나노", "나노 기술"
    "신원",      # 身元 — NO 4건. "해커 신원 공개"
    "미래산업",  # NO 7건. "미래 산업 육성" (단, 공시 목록의 실제 회사 언급은 쉼표 규칙으로 통과)
    "한창",      # 한창(부사) — "한창 진행 중"
}


def ambiguous_name_lacks_company_context(name: str, text: str) -> bool:
    """일반명사 동철 종목명이 '회사'로서 언급됐는지 판정. True = 회사 문맥 없음(차단).

    회사 문맥으로 인정하는 증거 2가지:
      ① 회사 접미 결합: "대상그룹", "대상홀딩스", "대상㈜", "대상(주)"
      ② 기사 관례의 주어 위치: 문두/구분자 직후 + 쉼표 — "대상, 2분기 영업익…"
    "…를 대상으로", "상장사 대상", "환자 대상" 같은 일반명사 용법은 둘 다 없다.

    트레이드오프: 회사 뉴스인데 제목 중간에 조사 없이 등장하는 드문 케이스를
    놓칠 수 있다(미탐). 미탐은 다음 스캔·다른 기사로 복구되지만 오탐은 실매수로
    이어지므로 엄격한 쪽을 택한다.
    """
    if _re.search(_re.escape(name) + r"\s?(그룹|홀딩스|㈜|\(주\))", text):
        return False
    if _re.search(r"(?:^|[\s\[\]\"'\u2018\u2019\u201c\u201d\u300c\u300e])"
                  + _re.escape(name) + r"\s*,", text):
        return False
    return True
